"use strict";
console.log("Statements and expressions");
const firstName = "Linda";

// Expressions produce a value
3 + 4;
1991;
true && false && !false;

// Statements do something and does not produce a value itself
if (23 > 10) {
  const str = "23 is bigger"; //the string '23 is bigger' is a expression because it produces a value
}

console.log(`I'm ${2023 - 1986} years old and my name is ${firstName}`);
